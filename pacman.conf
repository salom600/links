#
# /etc/pacman.conf
#
# Lumina Linux — pacman configuration used during ISO build.
# - Enables [multilib] for 32-bit gaming libraries (Steam, Wine)
# - Adds a local [lumina] repo containing AUR packages built by CI
# - Uses 4 parallel downloads for faster builds
#

[options]
HoldPkg           = pacman glibc
Architecture      = auto
CheckSpace
ParallelDownloads = 4
SigLevel          = Required DatabaseOptional
LocalFileSigLevel = Optional

# --- Lumina local AUR repo (built fresh by CI for each release) ---
[lumina]
SigLevel = Optional TrustAll
Server   = file:///usr/local/repo/lumina

[core]
Include = /etc/pacman.d/mirrorlist

[extra]
Include = /etc/pacman.d/mirrorlist

[multilib]
Include = /etc/pacman.d/mirrorlist

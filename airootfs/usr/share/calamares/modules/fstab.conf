# Lumina Linux — Calamares fstab module
---
# Generate /etc/fstab on the installed system.
mountOptions:
  - defaults
  - noatime
ssdOptions:
  - discard
btrfsOptions:
  - compress=zstd:1
  - noatime
